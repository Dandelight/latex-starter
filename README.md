# $\mathrm{\LaTeX}$ Starter

This is a template repository for $\mathrm{\LaTeX}$ with following features configured:

* `latexmk` as the default build system
* `chktex` configured for linting
* `devcontainer` for GitHub Codespace
